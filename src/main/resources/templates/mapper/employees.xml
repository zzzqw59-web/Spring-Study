<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.example.mapper.EmployeesMapper">
    <resultMap id="employeesResult" type="com.boot.example.domain.EmployeesDTO">
        <result column="employee_id" property="employeeID"></result>
        <result column="first_name" property="firstName"></result>
        <result column="last_name" property="lastName"></result>
        <result column="email" property="email"></result>
        <result column="phone_number" property="phoneNumber"></result>
        <result column="salary" property="salary"></result>
    </resultMap>

    <select id="employeesList" resultMap="employeesResult">
        SELECT employee_id, first_name, last_name, email, phone_number, salary
        FROM employees
    </select>

    <insert id="employeesInsert" parameterType="com.boot.example.domain.EmployeesDTO" useGeneratedKeys="true" keyColumn="employee_id" keyProperty="employeeID">
        INSERT INTO employees(employee_id, first_name, last_name, email, phone_number, salary)
        VALUES (#{employeeID}, #{firstName}, #{lastName}, #{email}, #{phoneNumber}, #{salary})
    </insert>


</mapper>