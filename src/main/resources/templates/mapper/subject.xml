<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.example.mapper.SubjectMapper">
    <resultMap id="subjectResult" type="com.boot.example.domain.SubjectDTO">
        <result column="no" property="no"></result>
        <result column="s_num" property="subjectNumber"></result>
        <result column="s_name" property="subjectName"></result>
    </resultMap>

    <select id="subjectList" resultMap="subjectResult">
        SELECT no, s_num, s_name
        FROM subject
    </select>

</mapper>